{
  "version": 2,
  "buildCommand": "CI=false NODE_ENV=production SKIP_PREFLIGHT_CHECK=true npm run build",
  "installCommand": "npm uninstall schema-utils && npm install --legacy-peer-deps --no-optional",
  "outputDirectory": "build",
  "framework": "create-react-app",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ],
  "env": {
    "REACT_APP_DATA_PATH": "/data/stanton_extract.json",
    "REACT_APP_DEFAULT_SYSTEM": "Stanton",
    "REACT_APP_VERSION": "0.1.0",
    "REACT_APP_TITLE": "Celestial Viewer",
    "NODE_OPTIONS": "--max-old-space-size=4096"
  }
} 